{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sliders list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Text</th>
                <th>Title</th>
                <th>Url</th>
                <th>Nameurl</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for slider in sliders %}
            <tr>
                <td><a href="{{ path('slider_show', { 'id': slider.id }) }}">{{ slider.id }}</a></td>
                <td>{{ slider.text }}</td>
                <td>{{ slider.title }}</td>
                <td>{{ slider.url }}</td>
                <td>{{ slider.nameUrl }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('slider_show', { 'id': slider.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('slider_edit', { 'id': slider.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('slider_new') }}">Create a new slider</a>
        </li>
    </ul>
{% endblock %}
