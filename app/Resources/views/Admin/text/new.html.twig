{% extends 'base.html.twig' %}

{% block body %}
    <h1>Text creation</h1>

    {{ form_start(form) }}
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_text_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
