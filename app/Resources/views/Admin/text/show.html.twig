{% extends 'base.html.twig' %}

{% block body %}
    <h1>Text</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ text.id }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ text.location }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ text.title }}</td>
            </tr>
            <tr>
                <th>Contents</th>
                <td>{{ text.contents }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_text_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_text_edit', { 'id': text.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
